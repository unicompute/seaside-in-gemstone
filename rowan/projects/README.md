This directory is reserved for Rowan project dependencies.

The initial Rowan scaffold for `SeasideInGemStone` does not yet encode
external dependencies such as Seaside or GemStonePharo as Rowan projects.
Those can be added here in a later pass once the local package grouping is
stable.
